import { SOIC } from "../src/SmallOutline"

export const SOIC2 = () => (
  <bug
    port_arrangement={{
      right_size: 6,
    }}
    port_labels={{}}
    footprint={<SOIC pad_count={6} row_count={1} />}
  />
)
