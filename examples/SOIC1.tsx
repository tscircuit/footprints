import { SOIC } from "../src/SmallOutline"

export const SOIC1 = () => (
  <bug
    port_arrangement={{
      left_size: 12,
      right_size: 12,
    }}
    port_labels={{}}
    footprint={<SOIC pad_count={6} />}
  />
)
