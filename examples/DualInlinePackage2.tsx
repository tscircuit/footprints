import { DualInlinePackage } from "../src/DualInlinePackage"

export const DualInlinePackage2 = () => (
  <bug
    port_arrangement={{
      left_size: 4,
      right_size: 4,
    }}
    port_labels={{}}
    footprint={<DualInlinePackage pad_count={8} />}
  />
)
